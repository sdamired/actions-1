name: my first actions 

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest 
        steps:
         - name: checkout
           uses: actions/checkout@v4
         - name: run the script
           run: |
             run: |
          chmod +x .github/workflows/ascii-script.sh
          .github/workflows/ascii-script.sh  # Corrected path